package com.sophossolutions.apis;

import io.cucumber.datatable.DataTable;
import io.vavr.collection.Map;
import net.serenitybdd.rest.SerenityRest;

import java.util.*;

public class Api {
	
	private static final String CONTENT_TYPE = "aplication/json";
	private static String baseUrl;
	
	public static void setBaseUrl(String baseUrl) {
		Api.baseUrl = baseUrl;
	}
	
	public static void ejecutarPost(String endPoint, DataTable infoUser) {
		
		//Mapeo tabla
		/*Map<String, String> body = (Map<String, String>) infoUser.asMap(String.class, String.class);*/
		
		SerenityRest.get("https://reqres.in/api/users?page=2").then().assertThat().statusCode(2);
		/*SerenityRest.given().contentType(CONTENT_TYPE).when().body(body).post(baseUrl.concat(endPoint));*/
		SerenityRest.lastResponse().prettyPeek();
	
	}
}
